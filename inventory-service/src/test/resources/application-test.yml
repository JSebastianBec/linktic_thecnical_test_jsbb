spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  rabbitmq:
    host: localhost
    port: 5672

security:
  api-key: test-key

product:
  service:
    url: http://localhost:8080
    api-key: test-key
